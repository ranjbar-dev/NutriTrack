# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
services:
  app:
    build:
      context: .
      target: builder  # Use builder stage for development
    environment:
      ENV: development
    volumes:
      - .:/app  # Mount source for hot reload (use with air or similar)
    command: go run ./cmd/server
