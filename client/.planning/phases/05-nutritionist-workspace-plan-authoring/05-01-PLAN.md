---
phase: 05-nutritionist-workspace-plan-authoring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/types/nutritionist-workspace.ts
  - app/types/diet-authoring.ts
  - app/types/catalogue.ts
  - app/types/food-request.ts
  - app/composables/useNutritionistClientApi.ts
  - app/composables/useDietPlanAuthoringApi.ts
  - app/composables/useCatalogueApi.ts
  - app/composables/useFoodRequestApi.ts
  - tests/nutritionist/workspace-api-contracts.spec.ts
autonomous: true
requirements:
  - NUTR-01
  - NUTR-02
  - NUTR-03
  - NUTR-04
  - CAT-01
  - CAT-02
  - CAT-03
must_haves:
  truths:
    - "Nutritionist workspace, diet-authoring, catalogue, and food-request endpoints are represented by typed contracts and composables."
    - "Composable APIs expose roster/profile, plan metadata/structure mutations, catalogue search, and request moderation operations with no implicit any typing."
    - "All new composables honor existing auth/session transport behavior and do not introduce backend-scope assumptions."
  artifacts:
    - path: app/types/nutritionist-workspace.ts
      provides: roster and profile response contracts
      exports: ["NutritionistClient", "NutritionistClientListResponse", "NutritionistClientStatusUpdateRequest"]
    - path: app/types/diet-authoring.ts
      provides: plan metadata and hierarchy mutation contracts
      exports: ["CreatePlanRequest", "PlanDay", "PlanMeal", "PlanOption", "PlanItem"]
    - path: app/types/catalogue.ts
      provides: shared food/medication/category contracts
      exports: ["FoodItem", "FoodCategory", "MedicationItem"]
    - path: app/types/food-request.ts
      provides: food request submit/review contracts
      exports: ["FoodRequest", "SubmitFoodRequestRequest", "ApproveFoodRequestRequest", "RejectFoodRequestRequest"]
    - path: app/composables/useNutritionistClientApi.ts
      provides: nutritionist client list/profile/status operations
      exports: ["useNutritionistClientApi"]
    - path: app/composables/useDietPlanAuthoringApi.ts
      provides: plan create/update and hierarchy mutation operations
      exports: ["useDietPlanAuthoringApi"]
    - path: app/composables/useCatalogueApi.ts
      provides: food/category/medication search operations
      exports: ["useCatalogueApi"]
    - path: app/composables/useFoodRequestApi.ts
      provides: client submit + nutritionist review operations
      exports: ["useFoodRequestApi"]
  key_links:
    - from: app/composables/useDietPlanAuthoringApi.ts
      to: app/types/diet-authoring.ts
      via: request/response typing imports
      pattern: "import.*diet-authoring"
    - from: app/composables/useCatalogueApi.ts
      to: app/types/catalogue.ts
      via: typed list response imports
      pattern: "import.*catalogue"
    - from: app/composables/useFoodRequestApi.ts
      to: app/types/food-request.ts
      via: submit/approve/reject typing imports
      pattern: "import.*food-request"
---

<objective>
Establish the API contract foundation for all Phase 5 UI work.

Purpose: reduce implementation ambiguity for waves 2-3 by locking endpoint contracts and composable interfaces before page/component construction.
Output: typed domain models plus composables for nutritionist workspace, plan authoring, catalogue access, and food-request workflow.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-nutritionist-workspace-plan-authoring/05-CONTEXT.md
@.planning/phases/05-nutritionist-workspace-plan-authoring/05-RESEARCH.md
@docs/API.md

<interfaces>
From app/composables/useMessagingApi.ts:
```typescript
export const useMessagingApi = () => {
  async function getNutritionistConversation(clientId: string, page = 1, pageSize = 50) {
    return useAsyncData(
      `nutritionist-conversation-${clientId}-${page}`,
      () => $fetch<PaginatedMessages>(`${baseUrl}/clients/${clientId}/messages?page=${page}&page_size=${pageSize}`)
    )
  }
}
```

From app/composables/useLabApi.ts:
```typescript
export const useLabApi = () => {
  async function listLabResults(clientId: string, page = 1, pageSize = 20) {
    return useAsyncData(
      `lab-results-${clientId}-${page}`,
      () => $fetch<PaginatedLabResults>(`${baseUrl}/clients/${clientId}/lab-results?page=${page}&page_size=${pageSize}`)
    )
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed contracts for workspace, authoring, catalogue, and food requests</name>
  <files>app/types/nutritionist-workspace.ts, app/types/diet-authoring.ts, app/types/catalogue.ts, app/types/food-request.ts</files>
  <behavior>
    - Test 1: Client roster and profile response types match docs/API.md section 10 (`GET /clients`, `GET /clients/:id`) per D-04.
    - Test 2: Plan hierarchy node types (day, meal, option, item, exercise, prescription) match docs/API.md section 14 per D-06.
    - Test 3: Food/medication/category and food-request contracts match sections 11, 12, 13, and 18 per D-09 and D-10.
  </behavior>
  <action>Create and export strict TypeScript interfaces for all Phase 5 endpoint payloads and envelopes. Include pagination meta wrappers where applicable. Keep field names API-faithful (snake_case where contract requires it) to avoid transform bugs.</action>
  <verify>
    <automated>npm run test:unit -- tests/nutritionist/workspace-api-contracts.spec.ts</automated>
  </verify>
  <done>All four type files compile and expose contract-faithful interfaces consumed by composables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement composables for nutritionist workspace and plan authoring endpoints</name>
  <files>app/composables/useNutritionistClientApi.ts, app/composables/useDietPlanAuthoringApi.ts</files>
  <behavior>
    - Test 1: `useNutritionistClientApi` exposes list/get/update/status methods for section 10 endpoints and typed pagination.
    - Test 2: `useDietPlanAuthoringApi` exposes create/update plan metadata plus add/delete operations for day, meal, option, item, exercise, prescription per D-07.
  </behavior>
  <action>Implement composables using existing `$fetch`/`useAsyncData` conventions with `baseUrl = '/api/v1'`. Keep operations granular and immutable (returning typed payloads without mutating shared objects in-place). Preserve auth behavior by relying on existing auth-fetch plugin, not custom headers.</action>
  <verify>
    <automated>npm run test:unit -- tests/nutritionist/workspace-api-contracts.spec.ts</automated>
  </verify>
  <done>Composable APIs provide complete typed operations for NUTR-01..NUTR-04 with zero TypeScript regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement composables for shared catalogue lookup and food-request workflow</name>
  <files>app/composables/useCatalogueApi.ts, app/composables/useFoodRequestApi.ts, tests/nutritionist/workspace-api-contracts.spec.ts</files>
  <behavior>
    - Test 1: Food and medication search composable methods support query + pagination and category filtering for foods.
    - Test 2: Food request composable supports client submit and nutritionist list/approve/reject endpoints with typed request bodies per D-03 and D-10.
  </behavior>
  <action>Implement read/search operations for shared catalogue and mutation operations for food request workflow. Ensure approve/reject methods accept explicit payloads and surface API errors without swallowing details.</action>
  <verify>
    <automated>npm run test:unit -- tests/nutritionist/workspace-api-contracts.spec.ts</automated>
  </verify>
  <done>Catalogue and request composables are production-usable and ready for downstream page/component plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nutritionist/client UI -> API | User input for search, plan editing, and request moderation crosses into authenticated API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | useDietPlanAuthoringApi | mitigate | Validate and strongly type all mutation payloads at composable boundary before API dispatch. |
| T-05-01-02 | Elevation of Privilege | useFoodRequestApi moderation methods | mitigate | Route moderation calls only from nutritionist namespace pages and keep role-guard middleware unchanged. |
| T-05-01-03 | Information Disclosure | catalogue and roster query methods | accept | Backend enforces resource ownership; frontend avoids caching cross-role data in shared global stores. |
</threat_model>

<verification>
1. `npm run test:unit -- tests/nutritionist/workspace-api-contracts.spec.ts`
2. `npx tsc --noEmit`
3. `rg "export const use(NutritionistClientApi|DietPlanAuthoringApi|CatalogueApi|FoodRequestApi)" app/composables`
</verification>

<success_criteria>
- New domain types and composables exist and compile.
- Endpoint coverage spans sections 10, 11, 12, 13, 14, and 18 of docs/API.md.
- Downstream plans can consume APIs without additional transport/contract discovery.
</success_criteria>

<output>
After completion, create `.planning/phases/05-nutritionist-workspace-plan-authoring/05-01-SUMMARY.md`
</output>