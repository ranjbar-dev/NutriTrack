---
phase: 01-platform-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-04
files_modified:
  - app/components/platform/InstallPromptBanner.vue
  - app/components/platform/UpdateAvailableBanner.vue
  - app/components/platform/ConnectivityBanner.vue
  - app/layouts/auth.vue
  - app/layouts/client.vue
  - app/layouts/nutritionist.vue
  - app/layouts/admin.vue
  - app/stores/platform-pwa.ts
  - tests/platform/pwa-update-prompt.spec.ts
  - tests/platform/cache-boundary.spec.ts
autonomous: true
requirements:
  - PLAT-02
must_haves:
  truths:
    - "User can see a clear non-blocking install prompt at an intentional moment, not on first paint."
    - "User can see a clear in-app update banner and trigger refresh when a new version is available."
    - "PWA UI wiring preserves safe cache boundaries and avoids broad caching of authenticated payloads."
  artifacts:
    - path: app/components/platform/InstallPromptBanner.vue
      provides: install prompt UX primitive
    - path: app/components/platform/UpdateAvailableBanner.vue
      provides: update available UX primitive
    - path: app/components/platform/ConnectivityBanner.vue
      provides: global connectivity status primitive
    - path: tests/platform/cache-boundary.spec.ts
      provides: automated guard for unsafe cache policy expansion
  key_links:
    - from: app/stores/platform-pwa.ts
      to: app/components/platform/UpdateAvailableBanner.vue
      via: needRefresh state -> update CTA
      pattern: "needRefresh|updateServiceWorker"
    - from: app/layouts/client.vue
      to: app/components/platform/InstallPromptBanner.vue
      via: intentional-moment banner render
      pattern: "showInstallPrompt|installPrompt"
---

<objective>
Complete the user-visible PWA experience layer for Phase 1 with safe prompt orchestration and cache-boundary guards.

Purpose: Fulfill install/update requirement without introducing intrusive UX or unsafe caching regressions.
Output: Reusable install/update/connectivity banners wired into role shells with automated policy checks.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-UI-SPEC.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-01-SUMMARY.md
@.planning/phases/01-platform-foundation/01-02-SUMMARY.md
@.planning/phases/01-platform-foundation/01-04-SUMMARY.md
@docs/PRD.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement install/update/connectivity platform banners</name>
  <files>app/components/platform/InstallPromptBanner.vue, app/components/platform/UpdateAvailableBanner.vue, app/components/platform/ConnectivityBanner.vue, app/stores/platform-pwa.ts</files>
  <behavior>
    - Test 1: Update banner appears when needRefresh is true and triggers update action.
    - Test 2: Install banner appears only after intentional app moment, not immediately on first paint.
  </behavior>
  <action>Create reusable Persian banner components for install, update, and connectivity status using non-blocking placement rules from UI-SPEC. Keep copy concise and actionable, and wire all banner state through platform-pwa store selectors to avoid ad-hoc layout logic.</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/pwa-update-prompt.spec.ts</automated>
  </verify>
  <done>Banners render correctly from store-driven state and respect intentional-timing and non-disruptive UX constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire prompt primitives into role layouts without violating role boundaries</name>
  <files>app/layouts/auth.vue, app/layouts/client.vue, app/layouts/nutritionist.vue, app/layouts/admin.vue</files>
  <behavior>
    - Test 1: Each layout receives only allowed platform banners for its context.
    - Test 2: Prompt rendering does not add cross-role navigation or route leakage.
  </behavior>
  <action>Integrate update/connectivity banners across role layouts, and place install prompt only at approved intentional moments. Preserve existing role shell isolation from prior plans and keep prompt components platform-level (not role-feature-specific).</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/shell-role-isolation.spec.ts</automated>
  </verify>
  <done>Prompts are visible where expected, absent where disallowed, and role shells remain isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression guard for PWA cache boundary expansion</name>
  <files>tests/platform/cache-boundary.spec.ts, tests/platform/pwa-update-prompt.spec.ts</files>
  <behavior>
    - Test 1: Fails if broad authenticated API runtime caching is introduced.
    - Test 2: Passes when static-shell allowlist boundaries remain intact.
  </behavior>
  <action>Add explicit tests that parse/validate effective cache policy assumptions and fail fast if patterns drift toward broad authenticated payload caching. Keep checks tied to Phase 1 safe-boundary constraints so later phases must opt in deliberately when changing policy.</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/cache-boundary.spec.ts</automated>
  </verify>
  <done>Automated tests prevent accidental broad caching of authenticated payloads while preserving install/update UX behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA runtime state -> install/update UI banners | Untrusted timing/state changes must not create deceptive or disruptive prompts |
| cache policy config -> offline runtime behavior | Misconfiguration can expose private payloads or stale sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | I | runtime cache boundaries | mitigate | enforce static-shell allowlist and add tests/platform/cache-boundary.spec.ts to fail on broad authenticated caching |
| T-01-08 | S | install/update prompt UX | mitigate | derive banner visibility from typed platform-pwa store state and block first-paint install auto-show |
| T-01-09 | R | update flow action handling | mitigate | require explicit user-triggered update action path and test needRefresh -> update CTA behavior |
</threat_model>

<verification>
- npm run lint
- npm run typecheck
- npm run test:unit -- tests/platform/pwa-update-prompt.spec.ts
- npm run test:unit -- tests/platform/cache-boundary.spec.ts
</verification>

<success_criteria>
- PLAT-02 is fully satisfied with clear install/update UX and non-intrusive behavior.
- Cache-boundary regressions are test-detectable.
- Phase 1 closes with safe PWA behavior aligned to frontend-only and role-isolated constraints.
</success_criteria>

<output>
After completion, create .planning/phases/01-platform-foundation/01-03-SUMMARY.md
</output>