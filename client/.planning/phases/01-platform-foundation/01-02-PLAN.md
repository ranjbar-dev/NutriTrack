---
phase: 01-platform-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/assets/css/main.css
  - app/lib/design/tokens.css
  - app/composables/useRtl.ts
  - app/composables/usePersianFormat.ts
  - app/lib/locale/numerals.ts
  - app/lib/locale/jalali.ts
  - app/components/platform/AppHeader.vue
  - app/components/platform/AppShell.vue
  - app/components/platform/BottomNavClient.vue
  - app/components/platform/InlineNotice.vue
  - app/components/platform/EmptyState.vue
  - app/components/platform/ErrorState.vue
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03
must_haves:
  truths:
    - "User sees Persian-first typography and clinical-minimal visual hierarchy across shared shell primitives."
    - "User sees Persian numerals by default and Jalali-aware date display in platform UI contexts."
    - "User experiences mobile safe-area aware shell spacing and role-appropriate navigation composition."
  artifacts:
    - path: app/lib/design/tokens.css
      provides: color, spacing, and typography tokens aligned to UI-SPEC
    - path: app/composables/usePersianFormat.ts
      provides: centralized Persian digit/date presentation helpers
    - path: app/components/platform/AppShell.vue
      provides: role-aware reusable shell frame
    - path: app/components/platform/BottomNavClient.vue
      provides: client-only bottom navigation primitive
  key_links:
    - from: app/components/platform/AppHeader.vue
      to: app/composables/useRtl.ts
      via: direction-aware UI behavior
      pattern: "dir|rtl"
    - from: app/components/platform/AppShell.vue
      to: app/lib/design/tokens.css
      via: tokenized layout classes and safe-area variables
      pattern: "safe-area|--space|--color"
---

<objective>
Implement the Persian RTL design baseline and shared shell primitives defined by the Phase 1 UI contract.

Purpose: Convert UI-SPEC into reusable platform primitives so all later role features inherit consistent Persian-native behavior.
Output: Tokenized design system, locale composables, and reusable shell components for role surfaces.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-UI-SPEC.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-01-SUMMARY.md
@docs/PRD.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement design tokens and base style primitives per UI-SPEC</name>
  <files>app/assets/css/main.css, app/lib/design/tokens.css</files>
  <behavior>
    - Test 1: Token values for spacing, palette, and typography match UI-SPEC baseline.
    - Test 2: Safe-area CSS variables are available to shell components.
  </behavior>
  <action>Define and wire platform CSS tokens using a clinical-minimal palette and restrained hierarchy per D-01. Configure Vazirmatn as the base Persian-first font per D-02 and map UI-SPEC spacing, typography, and state styles to reusable classes/utilities. Include safe-area variables and reduced-motion-safe transitions.</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/persian-locale-baseline.spec.ts</automated>
  </verify>
  <done>Token file and global styles enforce the UI-SPEC visual contract, including D-01 and D-02 constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Persian numeral and Jalali formatting composables</name>
  <files>app/composables/useRtl.ts, app/composables/usePersianFormat.ts, app/lib/locale/numerals.ts, app/lib/locale/jalali.ts</files>
  <behavior>
    - Test 1: Persian numerals are the default in display helpers.
    - Test 2: Jalali display helper returns stable localized output for known Gregorian dates.
  </behavior>
  <action>Create centralized RTL and locale formatting utilities that apply Persian numerals and Jalali display defaults per D-03, while preserving safe handling for explicit Latin-only inputs such as OTP and identifiers. Ensure helper APIs are composable-first and strict-typed for Nuxt 4 usage.</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/persian-locale-baseline.spec.ts</automated>
  </verify>
  <done>Locale helpers provide default Persian numeral and Jalali formatting and can be consumed consistently by shell components.</done>
</task>

<task type="auto">
  <name>Task 3: Implement shared platform shell primitives with role-aware navigation rules</name>
  <files>app/components/platform/AppHeader.vue, app/components/platform/AppShell.vue, app/components/platform/BottomNavClient.vue, app/components/platform/InlineNotice.vue, app/components/platform/EmptyState.vue, app/components/platform/ErrorState.vue</files>
  <action>Create reusable shell components aligned to the UI-SPEC composition rules: top app bar, role-aware shell wrapper, and client-only bottom nav primitive. Keep navigation role-isolated and mobile-first, enforce safe-area spacing, and include Persian copy-safe empty/error/notice primitives for downstream pages.</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/shell-role-isolation.spec.ts</automated>
  </verify>
  <done>Shell primitives are reusable, mobile-safe, and maintain role boundaries while reflecting the Persian visual baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale formatter input -> rendered UI | Untrusted/mixed-direction text must not break RTL readability |
| role shell props -> rendered nav | Navigation config must remain role-scoped to avoid cross-role disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | S | role-aware shell components | mitigate | require explicit role keys and block rendering of non-matching nav groups in AppShell and BottomNavClient |
| T-01-05 | T | numerals and date formatting helpers | mitigate | centralize conversions in usePersianFormat and cover with deterministic locale unit tests |
| T-01-06 | I | mixed-direction text rendering | mitigate | apply bidi-safe wrappers and RTL utility handling in shared text-bearing shell components |
</threat_model>

<verification>
- npm run lint
- npm run typecheck
- npm run test:unit -- tests/platform/persian-locale-baseline.spec.ts
- npm run test:unit -- tests/platform/shell-role-isolation.spec.ts
</verification>

<success_criteria>
- PLAT-03 Persian typography/numeral/Jalali/safe-area baseline is executable and test-covered.
- PLAT-01 shell primitives remain role-isolated and reusable.
- D-01, D-02, D-03 are implemented explicitly in foundation artifacts.
</success_criteria>

<output>
After completion, create .planning/phases/01-platform-foundation/01-02-SUMMARY.md
</output>
