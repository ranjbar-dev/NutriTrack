---
phase: 01-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - nuxt.config.ts
  - app/plugins/pwa.client.ts
  - app/stores/platform-pwa.ts
  - vitest.config.ts
  - playwright.config.ts
  - tests/platform/shell-role-isolation.spec.ts
  - tests/platform/pwa-update-prompt.spec.ts
  - tests/platform/persian-locale-baseline.spec.ts
autonomous: true
requirements:
  - PLAT-02
must_haves:
  truths:
    - "User can install the app and receive update-state signals from PWA runtime state."
  artifacts:
    - path: package.json
      provides: test and build scripts for platform baseline verification
    - path: nuxt.config.ts
      provides: Nuxt modules and conservative PWA configuration
  key_links:
    - from: nuxt.config.ts
      to: app/plugins/pwa.client.ts
      via: vite-pwa module registration and runtime flags
      pattern: "vitePwa|pwa|workbox"
    - from: app/middleware/role-shell.global.ts
      to: app/pages/client/index.vue
      via: route namespace checks
      pattern: "to.path.startsWith('/client')"
---

<objective>
Establish the executable platform baseline: Nuxt/Tailwind/Pinia/PWA wiring and automated test scaffolding for Phase 1.

Purpose: Build non-negotiable platform constraints first so later feature phases do not require cache boundary rewrites.
Output: Running Nuxt foundation with safe PWA baseline and initial automated checks.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-UI-SPEC.md
@docs/PRD.md
@docs/API.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize platform test and verification harness</name>
  <files>package.json, vitest.config.ts, playwright.config.ts, tests/platform/shell-role-isolation.spec.ts, tests/platform/pwa-update-prompt.spec.ts, tests/platform/persian-locale-baseline.spec.ts</files>
  <action>Create test scripts and baseline platform test files before production implementation so all later tasks have executable checks. Include unit coverage for role shell isolation and locale baseline, and add one PWA prompt smoke test placeholder with deterministic mocking. If no implementation exists yet, keep tests narrowly scoped to shell contracts and fallback states.</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/shell-role-isolation.spec.ts</automated>
  </verify>
  <done>Test harness runs locally and at least one shell-isolation test executes successfully with current scaffold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Nuxt modules and conservative PWA runtime boundaries</name>
  <files>nuxt.config.ts, app/app.vue, app/plugins/pwa.client.ts, app/stores/platform-pwa.ts</files>
  <behavior>
    - Test 1: PWA install/update flags become available through a typed store/composable surface.
    - Test 2: Service worker configuration does not cache authenticated API payloads broadly.
  </behavior>
  <action>Configure Nuxt 4 with Tailwind CSS 4, Pinia, and @vite-pwa/nuxt. Implement conservative runtime caching (static assets/shell only) and explicitly exclude authenticated API payload patterns from broad runtime caching, per Phase constraint on safe boundaries. Add typed client plugin/store bridge for install and update flags. Keep prompt state non-blocking and reusable by layouts.</action>
  <verify>
    <automated>npm run test:unit -- tests/platform/pwa-update-prompt.spec.ts</automated>
  </verify>
  <done>PWA module is active, prompt state is consumable in UI, and cache config contains explicit authenticated payload protections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker runtime cache -> authenticated API responses | Cache policy boundary must prevent private payload over-caching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | PWA runtime cache policy | mitigate | allowlist static assets only and exclude authenticated API payload patterns in nuxt.config.ts workbox rules |
| T-01-02 | T | PWA prompt state bridge | mitigate | type prompt flags in app/stores/platform-pwa.ts and sanitize prompt-trigger transitions in pwa plugin |
</threat_model>

<verification>
- npm run lint
- npm run typecheck
- npm run test:unit -- tests/platform/pwa-update-prompt.spec.ts
</verification>

<success_criteria>
- PLAT-02 technical baseline (install/update state + safe cache policy) is in place.
- Phase 1 has deterministic automated checks that later plans can extend.
</success_criteria>

<output>
After completion, create .planning/phases/01-platform-foundation/01-01-SUMMARY.md
</output>