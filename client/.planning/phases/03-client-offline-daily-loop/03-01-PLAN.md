---
phase: 03-client-offline-daily-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/types/tracking.ts
  - app/types/offline-sync.ts
  - app/composables/useClientPlanApi.ts
  - app/composables/useTrackingApi.ts
  - app/stores/client-offline.ts
  - app/plugins/client-sync.client.ts
  - tests/client/offline-queue-state.spec.ts
  - tests/client/sync-replay.spec.ts
autonomous: true
requirements:
  - OFFL-02
  - OFFL-03
must_haves:
  truths:
    - "Client tracking writes are saved locally with a durable local_id and visible sync state before network success."
    - "Queued entries replay on reconnect and app-open without creating duplicates."
    - "Failed sync entries remain recoverable for manual retry."
  artifacts:
    - path: app/stores/client-offline.ts
      provides: queue persistence and per-entry sync state transitions
    - path: app/composables/useTrackingApi.ts
      provides: typed tracking endpoint contracts and local_id-safe payload builders
    - path: app/plugins/client-sync.client.ts
      provides: reconnect/app-open replay triggers and retry orchestration
  key_links:
    - from: app/stores/client-offline.ts
      to: app/composables/useTrackingApi.ts
      via: queue item to endpoint dispatch mapping
      pattern: "food|water|sleep|exercise|medication|body"
    - from: app/plugins/client-sync.client.ts
      to: app/stores/client-offline.ts
      via: online/app-open event replay
      pattern: "queued|failed -> syncing"
---

<objective>
Create the offline tracking and sync foundation used by all Phase 3 client surfaces.

Purpose: ensure client writes are durable and recoverable under unstable connectivity before UI flow expansion.
Output: typed tracking contracts, offline queue store, and replay plugin with regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-client-offline-daily-loop/03-CONTEXT.md
@.planning/phases/03-client-offline-daily-loop/03-UI-SPEC.md
@docs/API.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed tracking contracts and queue-safe API composables</name>
  <files>app/types/tracking.ts, app/types/offline-sync.ts, app/composables/useTrackingApi.ts, app/composables/useClientPlanApi.ts</files>
  <behavior>
    - Test 1: Every tracking payload type requires local_id and maps to the documented endpoint request body.
    - Test 2: Bulk sync payload supports food, water, sleep, exercise, medication, and body entry variants.
  </behavior>
  <action>Implement strict TypeScript contracts for plan reads and tracking writes using docs/API.md endpoint shapes, including idempotent local_id handling (per D-04, D-05). Build composables for individual tracking endpoints and /tracking/sync with explicit domain mapping and no implicit any typing.</action>
  <verify>
    <automated>npm run test:unit -- tests/client/offline-queue-state.spec.ts</automated>
  </verify>
  <done>Tracking and sync requests are typed, local_id is required, and endpoint routing is deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable client queue store and replay orchestration</name>
  <files>app/stores/client-offline.ts, app/plugins/client-sync.client.ts, tests/client/offline-queue-state.spec.ts, tests/client/sync-replay.spec.ts</files>
  <behavior>
    - Test 1: New offline write enters queue with state queued, transitions to syncing during replay, and to synced/failed by result.
    - Test 2: Reconnect and app-open each trigger replay while preventing duplicate concurrent sync loops.
  </behavior>
  <action>Build client-only offline queue persistence with explicit states queued/syncing/synced/failed and immutable transitions (per D-04, D-05, D-08). Add replay triggers on reconnect and app-open plus manual retry hooks for failed entries, using last-write-wins assumptions while surfacing failure records for UX recovery (per D-06, D-07).</action>
  <verify>
    <automated>npm run test:unit -- tests/client/offline-queue-state.spec.ts tests/client/sync-replay.spec.ts</automated>
  </verify>
  <done>Queue persistence, state transitions, and replay triggers are implemented and regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement authenticated session lifecycle cleanup on logout</name>
  <files>app/stores/client-offline.ts, app/plugins/auth-bootstrap.client.ts, tests/client/offline-queue-state.spec.ts</files>
  <behavior>
    - Test 1: On logout, all queue entries are deleted from Dexie client-offline database.
    - Test 2: On logout, all plan cache tables are cleared.
    - Test 3: On logout, sync state counters (queued, syncing) are reset to zero.
  </behavior>
  <action>Hook into auth-session.ts logout action and trigger full offline state purge before redirect. Ensure cleanup happens atomically to prevent previous-user data exposure on shared devices (per D-10). Add explicit delete transactions and verification that no tables remain after logout.</action>
  <verify>
    <automated>npm run test:unit -- tests/client/offline-queue-state.spec.ts</automated>
  </verify>
  <done>Logout cleanup is implemented and user-scoped offline data is fully isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted client input -> queue persistence | User-entered tracking payloads must be validated before local storage and sync dispatch. |
| local queue -> tracking API | Offline payload replay must not send malformed or cross-domain payload types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | app/stores/client-offline.ts | mitigate | Validate queue entry schema and freeze state transitions to allowed edges only (queued->syncing->synced/failed). |
| T-03-02 | D | app/plugins/client-sync.client.ts | mitigate | Add single-flight replay guard to block overlapping reconnect/app-open sync storms. |
| T-03-03 | I | app/composables/useTrackingApi.ts | mitigate | Restrict payload serialization to typed fields and avoid persisting tokens or sensitive auth metadata in queue records. |
| T-03-04 | E | queue domain mapper | mitigate | Enforce client-only tracking domain allowlist so nutritionist/admin traffic never enters offline queue flow (per D-08). |
| T-03-05 | S | logout cleanup | mitigate | Verify all offline tables deleted atomically on logout; no stale data accessible after session ends (per D-10). |
</threat_model>

<verification>
- npm run lint
- npm run test:unit -- tests/client/offline-queue-state.spec.ts tests/client/sync-replay.spec.ts
</verification>

<success_criteria>
- OFFL-02 durable queue contract and local_id-based write persistence are implemented.
- OFFL-03 replay and retry foundation is implemented for reconnect, app-open, and failed entries.
</success_criteria>

<output>
After completion, create .planning/phases/03-client-offline-daily-loop/03-01-SUMMARY.md
</output>
