---
phase: 03-client-offline-daily-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/components/client/SyncStatusStrip.vue
  - app/components/client/SyncStateChip.vue
  - app/components/client/TodayPlanSnapshotCard.vue
  - app/components/client/PendingActionsCard.vue
  - app/components/client/WaterQuickAdd.vue
  - app/pages/client/index.vue
  - app/layouts/client.vue
  - tests/client/today-view-shell.spec.ts
  - tests/client/sync-strip-visibility.spec.ts
autonomous: true
requirements:
  - CLNT-01
  - OFFL-01
  - OFFL-03
must_haves:
  truths:
    - "Client Today view shows active plan snapshot, pending actions, water progress, and sync status in one screen."
    - "Sync state is always visible in client shell and explains recovery steps in Persian copy."
    - "Today essentials remain readable from cached data while offline."
  artifacts:
    - path: app/pages/client/index.vue
      provides: action-oriented Today composition
    - path: app/components/client/SyncStatusStrip.vue
      provides: persistent aggregate queue status and retry CTA
    - path: app/layouts/client.vue
      provides: shell-level sync strip placement under connectivity banner
  key_links:
    - from: app/layouts/client.vue
      to: app/components/client/SyncStatusStrip.vue
      via: persistent client-shell mounting
      pattern: "connectivity banner -> sync strip -> page content"
    - from: app/pages/client/index.vue
      to: app/stores/client-offline.ts
      via: pending actions and water quick-add state binding
      pattern: "queued|failed counters and daily progress"
---

<objective>
Deliver the client Today surface with first-class sync visibility and offline-safe essentials.

Purpose: fulfill the Phase 3 trust and action loop by putting plan snapshot, quick tracking actions, and sync transparency at the center of /client.
Output: Today view components, shell sync strip wiring, and tests for visibility and offline behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-client-offline-daily-loop/03-CONTEXT.md
@.planning/phases/03-client-offline-daily-loop/03-UI-SPEC.md
@.planning/phases/03-client-offline-daily-loop/03-01-SUMMARY.md
@docs/API.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Today action cards and offline-readable plan snapshot</name>
  <files>app/pages/client/index.vue, app/components/client/TodayPlanSnapshotCard.vue, app/components/client/PendingActionsCard.vue, app/components/client/WaterQuickAdd.vue, tests/client/today-view-shell.spec.ts</files>
  <behavior>
    - Test 1: Today page renders active plan snapshot, pending tracking action buckets, and water quick-add controls.
    - Test 2: When network is offline with cached active plan, snapshot remains visible with "آخرین به روزرسانی: HH:MM" timestamp. Pending counts derive only from queued entries (not synced upstream data).
    - Test 3: Untracked lookups (e.g., food categories for new entries) require online; show "اتصال اینترنت را بررسی کنید" when unavailable.
  </behavior>
  <action>Implement an action-first Today layout (per D-01, D-03, D-09) using cached active plan and queue-derived pending counts. Keep one-thumb quick-add interaction for water and short Persian guidance when data is stale (per D-11, D-12 and UI-SPEC offline read contract).</action>
  <verify>
    <automated>npm run test:unit -- tests/client/today-view-shell.spec.ts</automated>
  </verify>
  <done>Today screen provides active plan context, pending actions, and water quick actions online/offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add persistent sync strip and per-state chip components in client shell</name>
  <files>app/components/client/SyncStatusStrip.vue, app/components/client/SyncStateChip.vue, app/layouts/client.vue, tests/client/sync-strip-visibility.spec.ts</files>
  <behavior>
    - Test 1: Sync strip appears below connectivity banner with queued/syncing/synced/failed aggregate state.
    - Test 2: Failed state remains visible with manual retry CTA and non-technical Persian recovery text.
  </behavior>
  <action>Implement shell-level sync status strip and reusable row chip with explicit queued/syncing/synced/failed labels (per D-05, D-11). Keep failure state persistent until resolved and provide clear manual retry path with plain-language Persian copy (per D-06, D-12).</action>
  <verify>
    <automated>npm run test:unit -- tests/client/sync-strip-visibility.spec.ts</automated>
  </verify>
  <done>Sync state is always discoverable in client shell and aligns with offline trust UX contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue state -> user-facing status UI | Internal sync state must map accurately to user-visible status and actions. |
| cached plan data -> Today screen rendering | Stale cached data must be labeled to avoid misleading health guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | I | app/components/client/SyncStatusStrip.vue | mitigate | Render explicit state labels and last-sync timestamp to prevent ambiguous or hidden sync failures. |
| T-03-12 | T | app/pages/client/index.vue | mitigate | Bind pending metrics only to typed store selectors; block ad-hoc computed state that can drift from queue truth. |
| T-03-13 | R | sync retry CTA flow | mitigate | Keep retry action tied to queue metadata and preserve auditable per-entry state transitions in store. |
</threat_model>

<verification>
- npm run lint
- npm run test:unit -- tests/client/today-view-shell.spec.ts tests/client/sync-strip-visibility.spec.ts
</verification>

<success_criteria>
- CLNT-01 Today view contract is implemented with plan snapshot, pending actions, water target, and sync visibility.
- OFFL-01 and OFFL-03 UX transparency requirements are implemented for cached reads and failure recovery cues.
</success_criteria>

<output>
After completion, create .planning/phases/03-client-offline-daily-loop/03-02-SUMMARY.md
</output>
