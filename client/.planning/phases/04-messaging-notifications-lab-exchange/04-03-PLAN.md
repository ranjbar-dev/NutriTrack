---
phase: 04-messaging-notifications-lab-exchange
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/pages/nutritionist/messages/index.vue
  - app/pages/nutritionist/messages/[clientId].vue
  - app/components/nutritionist/ClientConversationItem.vue
  - app/components/nutritionist/NutritionistMessageThread.vue
  - app/components/nutritionist/NutritionistMessageComposeBar.vue
  - tests/nutritionist/messaging-conversations.spec.ts
autonomous: true
requirements:
  - MSG-01
  - MSG-02

must_haves:
  truths:
    - "Nutritionist can open /nutritionist/messages and see a list of client conversations with unread counts"
    - "Nutritionist can select a client and open the conversation thread at /nutritionist/messages/:clientId"
    - "Conversation thread polls every 10 seconds while open; stops on unmount"
    - "Nutritionist can send text and file attachments from the conversation screen"
    - "Unread badge shows on each client conversation item when unread_count > 0"
  artifacts:
    - path: "app/pages/nutritionist/messages/index.vue"
      provides: "Nutritionist client conversation list with unread indicators"
    - path: "app/pages/nutritionist/messages/[clientId].vue"
      provides: "Per-client conversation thread with polling and compose"
    - path: "app/components/nutritionist/ClientConversationItem.vue"
      provides: "Client list item with unread badge"
    - path: "app/components/nutritionist/NutritionistMessageThread.vue"
      provides: "Conversation thread (re-uses Message types from Plan 04-01)"
    - path: "app/components/nutritionist/NutritionistMessageComposeBar.vue"
      provides: "Nutritionist compose bar (same constraints as client)"
  key_links:
    - from: "app/pages/nutritionist/messages/[clientId].vue"
      to: "app/composables/useMessagingApi.ts"
      via: "getNutritionistConversation, sendNutritionistMessage"
      pattern: "useMessagingApi"
    - from: "app/pages/nutritionist/messages/index.vue"
      to: "app/composables/useMessagingApi.ts"
      via: "getUnreadCount"
      pattern: "getUnreadCount"
---

<objective>
Deliver the nutritionist conversation surfaces: a client-list messages index showing unread indicators and a per-client conversation thread with polling and compose.

Purpose: Satisfies MSG-01 and MSG-02 for the nutritionist role. No offline queue logic needed on the nutritionist side (PRD scopes offline capability to client only).

Output:
- Nutritionist messages index page (client list + unread badges)
- Per-client conversation page with 10s polling
- NutritionistMessageThread and NutritionistMessageComposeBar components
- ClientConversationItem list item component
- Test coverage for nutritionist conversation rendering and unread state
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-messaging-notifications-lab-exchange/04-RESEARCH.md
@.planning/phases/04-messaging-notifications-lab-exchange/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 04-01 -->

From app/types/messaging.ts:
```typescript
export interface Message { id: string; sender_id: string; receiver_id: string; content: string | null; is_mine: boolean; read_at: string | null; attachment: MessageAttachment | null; created_at: string }
export interface MessageAttachment { url: string; type: string; size: number; name: string }
export interface SendMessageRequest { content?: string; file?: File }
export interface PaginatedMessages { data: Message[]; total: number; page: number; page_size: number }
```

From app/composables/useMessagingApi.ts:
```typescript
export const useMessagingApi = () => ({
  getNutritionistConversation: (clientId: string, page?: number, pageSize?: number) => useAsyncData<PaginatedMessages>(...),
  sendNutritionistMessage: (clientId: string, req: SendMessageRequest) => Promise<Message>,
  getUnreadCount: () => Promise<UnreadCountResponse>,
})
```

Note: Nutritionist message screens are online-only. No offline store access needed.
useRuntimeConfig().public route convention: nutritionist routes are under /nutritionist/** and use nutritionist.vue layout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClientConversationItem component and messages index page</name>
  <files>app/components/nutritionist/ClientConversationItem.vue, app/pages/nutritionist/messages/index.vue, tests/nutritionist/messaging-conversations.spec.ts</files>
  <behavior>
    ClientConversationItem:
    - Props: { clientId: string; clientName: string; lastMessagePreview: string | null; unreadCount: number; lastMessageAt: string | null }
    - Renders client name, last message preview (truncated at 40 chars with "..."), and Jalali-formatted lastMessageAt (use usePersianFormat composable)
    - When unreadCount > 0: shows a badge chip (class "unread-badge") with the count; badge is hidden when unreadCount === 0
    - Entire item is a NuxtLink to /nutritionist/messages/:clientId
    - RTL layout: client name right-aligned, unread badge left-aligned (flex row-reverse)

    messages/index.vue:
    - Fetches client list from existing nutritionist clients endpoint (NOTE: /clients endpoint is Phase 5 scope — use a placeholder empty list with a TODO comment; the component structure must be complete)
    - For each client in list: fetch unread count from GET /messages/unread-count... (NOTE: The unread-count endpoint returns a single count, not per-client. The index page shows the global unread count in the nav. Per-client unread state is determined by having unread messages in the conversation. For the index: show ClientConversationItem for each client with unreadCount derived from last known message read_at state — initialize as 0, mark via polling in individual conversation pages)
    - Shows EmptyState when client list is empty with label "هیچ مکالمه‌ای وجود ندارد"
    - Uses nutritionist.vue layout
    - Navigates to /nutritionist/messages/:clientId on item click (handled by NuxtLink in item)

    Test in tests/nutritionist/messaging-conversations.spec.ts:
    - Test ClientConversationItem renders client name
    - Test unread badge visible when unreadCount > 0, hidden when 0
    - Test last message preview truncates at 40 chars
  </behavior>
  <action>
    Create tests/nutritionist/ directory (new directory for nutritionist-scoped tests).

    Create app/components/nutritionist/ClientConversationItem.vue:
    - Script setup with defined props (clientId, clientName, lastMessagePreview, unreadCount, lastMessageAt)
    - Import usePersianFormat for date formatting
    - Template: NuxtLink :to="`/nutritionist/messages/${clientId}`" with RTL flex layout
    - Unread badge: v-if="unreadCount > 0" span with class "unread-badge" showing unreadCount
    - Preview text: computed truncation of lastMessagePreview (slice(0, 40) + '...' when longer)
    - Scoped styles for RTL flex layout and badge styling (background var(--color-primary))

    Create app/pages/nutritionist/messages/index.vue:
    - definePageMeta({ layout: 'nutritionist' })
    - Script: ref clients = [] (TODO: wire to Phase 5 client roster API); unreadCounts ref<Record<string, number>> initialized as {}
    - Template: AppShell title "پیام‌ها", v-for ClientConversationItem with :clientId, :clientName, :unreadCount="unreadCounts[client.id] ?? 0", :lastMessagePreview, :lastMessageAt
    - EmptyState when clients is empty
    - Add comment: // TODO(Phase 5): wire clients to GET /clients nutritionist roster

    Create tests/nutritionist/messaging-conversations.spec.ts:
    - Test ClientConversationItem: mount with unreadCount=3 → badge visible; unreadCount=0 → badge not rendered
    - Test preview truncation: lastMessagePreview of 60 chars → rendered preview ≤ 43 chars (40 + '...')
    - Test NuxtLink href resolves to /nutritionist/messages/:clientId
  </action>
  <verify>
    <automated>npx vitest run tests/nutritionist/messaging-conversations.spec.ts</automated>
  </verify>
  <done>
    - ClientConversationItem renders unread badge when unreadCount > 0
    - Preview text truncated at 40 chars
    - messages/index.vue uses nutritionist layout and shows EmptyState
    - Specified tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nutritionist per-client conversation page with polling and compose</name>
  <files>app/pages/nutritionist/messages/[clientId].vue, app/components/nutritionist/NutritionistMessageThread.vue, app/components/nutritionist/NutritionistMessageComposeBar.vue</files>
  <behavior>
    NutritionistMessageThread:
    - Props: { messages: Message[] }
    - Reverses API newest-first to oldest-first for display (same as MessageThread in Plan 04-02)
    - Shows EmptyState when no messages ("هنوز پیامی ارسال نشده است")
    - Renders each message as a bubble:
      - is_mine: true → right-aligned; is_mine: false → left-aligned
      - Attachment: show name as link if attachment.url present
      - No SyncStateChip (nutritionist surface is online-only)
    - Scroll-to-bottom on message array update

    NutritionistMessageComposeBar:
    - Props: none; Emits: send({ content?: string; file?: File })
    - Same file validation constraints as client compose bar:
      - Images (JPG/PNG) max 5MB — error: "حداکثر حجم تصویر ۵ مگابایت است"
      - PDF max 10MB — error: "حداکثر حجم فایل ۱۰ مگابایت است"
    - Persian placeholder: "پیام بنویسید..."
    - Send disabled when both text empty and no file selected

    [clientId].vue (nutritionist per-client conversation):
    - useRoute() to get clientId param
    - On mount: initial load via getNutritionistConversation(clientId); set 10s polling interval
    - On unmount: clearInterval
    - On send from compose bar:
      - Online + text/file: sendNutritionistMessage(clientId, { content, file })
      - Show optimistic message while sending; replace with server response on success
      - On send failure: show InlineNotice "ارسال پیام ناموفق بود — دوباره تلاش کنید"
    - Page title: client name if available, fallback "مکالمه"
    - Uses nutritionist.vue layout
    - Back navigation to /nutritionist/messages
  </behavior>
  <action>
    Create app/components/nutritionist/NutritionistMessageThread.vue:
    - Script setup with props: { messages: Message[] }
    - Import Message from '~/types/messaging'
    - Computed reversed: [...props.messages].reverse()
    - Template: scroll container, v-for over reversed (key by message.id), inline bubble divs with conditional RTL/LTR class, attachment link when present, EmptyState fallback
    - Watch on reversed.length → scrollToBottom via templateRef

    Create app/components/nutritionist/NutritionistMessageComposeBar.vue:
    - Script setup, emit(['send'])
    - Refs: text, selectedFile, validationError
    - File validation function: check file.type and file.size; set validationError if invalid
    - Template: textarea (RTL), file input (hidden), file picker trigger button, send button, InlineNotice for validationError
    - Send handler: emit('send', { content: text || undefined, file: selectedFile || undefined }); reset refs

    Create app/pages/nutritionist/messages/[clientId].vue:
    - definePageMeta({ layout: 'nutritionist' })
    - const route = useRoute(); const clientId = computed(() => route.params.clientId as string)
    - useMessagingApi: destructure getNutritionistConversation, sendNutritionistMessage
    - messages ref<Message[]>; sendError ref<string|null>
    - Initial load on mount; setInterval 10s for $fetch refresh; clearInterval on unmount
    - handleSend: await sendNutritionistMessage(clientId.value, req); append to messages; catch → set sendError
    - Template: AppShell with back button to /nutritionist/messages, NutritionistMessageThread, NutritionistMessageComposeBar, InlineNotice for sendError
  </action>
  <verify>
    <automated>npx vitest run tests/nutritionist/messaging-conversations.spec.ts</automated>
  </verify>
  <done>
    - /nutritionist/messages/:clientId renders conversation with polling
    - Compose bar validates file sizes with Persian errors
    - Send failure shows InlineNotice
    - NutritionistMessageThread reverses messages to chronological order
    - All nutritionist messaging tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutritionist → API | sendNutritionistMessage uses clientId from route param as path variable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | /nutritionist/messages/* routes | mitigate | role-shell.global.ts middleware (Phase 2) prevents client users from reaching nutritionist routes; no additional guard needed in this plan |
| T-04-03-02 | Tampering | clientId route param in sendNutritionistMessage | accept | Backend validates nutritionist-client relationship via JWT; frontend sends the id from route, backend enforces ownership; no frontend IDOR mitigation possible beyond using the authenticated API |
| T-04-03-03 | Tampering | NutritionistMessageComposeBar file input | mitigate | Same client-side MIME + size validation as client compose bar (D-03); reject before upload |
</threat_model>

<verification>
1. `npx vitest run tests/nutritionist/messaging-conversations.spec.ts` — all pass
2. `npx tsc --noEmit` — no errors
3. Manual: navigate to /nutritionist/messages → client list renders → open conversation → messages display → send works
</verification>

<success_criteria>
- /nutritionist/messages uses nutritionist layout, shows client list with EmptyState fallback
- /nutritionist/messages/:clientId polls at 10s, stops on unmount
- Attachment validation: image >5MB, PDF >10MB each show Persian error before send
- All nutritionist messaging tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-messaging-notifications-lab-exchange/04-03-SUMMARY.md`
</output>
