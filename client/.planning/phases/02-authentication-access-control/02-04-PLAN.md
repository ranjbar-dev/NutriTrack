---
phase: 02-authentication-access-control
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - app/middleware/role-shell.global.ts
  - app/middleware/auth-access.global.ts
  - app/plugins/auth-bootstrap.client.ts
  - app/layouts/auth.vue
  - tests/auth/route-access-control.spec.ts
  - tests/auth/session-expiry-redirect.spec.ts
  - tests/platform/shell-role-isolation.spec.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "Users can access only routes allowed for their role namespace on direct URL entry, refresh, and route transitions."
    - "Expired or revoked sessions redirect users safely to role-appropriate auth entry."
    - "Authenticated users opening auth routes are redirected to their role home route."
  artifacts:
    - path: app/middleware/auth-access.global.ts
      provides: role-aware deny-by-default route access control
    - path: app/plugins/auth-bootstrap.client.ts
      provides: startup auth restoration and me-check bootstrap
    - path: tests/auth/route-access-control.spec.ts
      provides: deterministic route guard regression coverage
  key_links:
    - from: app/middleware/auth-access.global.ts
      to: app/middleware/role-shell.global.ts
      via: namespace and role guard composition
      pattern: "deny-by-default namespace check"
    - from: app/plugins/auth-bootstrap.client.ts
      to: app/stores/auth-session.ts
      via: bootstrap restore and forced-logout redirect
      pattern: "session bootstrap on app startup"
---

<objective>
Finalize authentication access control boundaries and redirect behavior across all navigation modes.

Purpose: close Phase 2 with enforceable route-level security so role identity and namespace permissions remain consistent under deep links, refresh, and transition events.
Output: global access middleware, bootstrap wiring, and regression tests for route access and session-expiry redirects.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-access-control/02-CONTEXT.md
@.planning/phases/02-authentication-access-control/02-UI-SPEC.md
@.planning/phases/02-authentication-access-control/02-01-SUMMARY.md
@.planning/phases/02-authentication-access-control/02-02-SUMMARY.md
@.planning/phases/02-authentication-access-control/02-03-SUMMARY.md
@docs/API.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce deny-by-default role namespace access in global middleware</name>
  <files>app/middleware/auth-access.global.ts, app/middleware/role-shell.global.ts, tests/auth/route-access-control.spec.ts, tests/platform/shell-role-isolation.spec.ts</files>
  <behavior>
    - Test 1: Role mismatch access attempts to /client, /nutritionist, or /admin are denied and redirected safely.
    - Test 2: Guard checks run consistently on direct entry, refresh bootstrap, and route navigation.
  </behavior>
  <action>Implement global auth-access middleware that composes with existing role-shell guard to enforce strict namespace policy (per D-07) on direct URL entry, refresh, and transitions (per D-09). Keep default posture deny-by-default and avoid public exposure of private role navigation on auth surfaces (per D-08).</action>
  <verify>
    <automated>npm run test:unit -- tests/auth/route-access-control.spec.ts tests/platform/shell-role-isolation.spec.ts</automated>
  </verify>
  <done>Role routing is enforced deterministically with deny-by-default behavior across all navigation modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire bootstrap and session-expiry redirect rules</name>
  <files>app/plugins/auth-bootstrap.client.ts, app/layouts/auth.vue, tests/auth/session-expiry-redirect.spec.ts</files>
  <behavior>
    - Test 1: Expired or revoked token states redirect to role-appropriate auth route with session-expired notice.
    - Test 2: Authenticated user visiting /auth routes is redirected to role root unless in logout/session-expired handoff.
  </behavior>
  <action>Implement startup auth bootstrap and redirect matrix aligned with UI spec and API error codes so INVALID_TOKEN/TOKEN_REVOKED/UNAUTHORIZED produce safe logout and role-auth handoff (per D-05). Preserve logout contract through /auth/logout and state clearing path from Plan 02-01 (per D-06).</action>
  <verify>
    <automated>npm run test:unit -- tests/auth/session-expiry-redirect.spec.ts</automated>
  </verify>
  <done>Session expiry and auth-route redirection are consistent, role-aware, and regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route navigation intent -> middleware decisions | Untrusted route targets must pass explicit role and session checks. |
| persisted session bootstrap -> runtime routing | Restored session state must be validated before granting route access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | E | auth-access middleware | mitigate | Enforce explicit role-to-namespace matrix and deny-by-default fallback for unknown routes. |
| T-02-12 | S | session bootstrap plugin | mitigate | Validate restored role/token pair before route unlock and force re-auth on inconsistency. |
| T-02-13 | D | redirect loop handling | mitigate | Add redirect-loop guard with idempotent target normalization to avoid repeated auth bounce. |
</threat_model>

<verification>
- npm run lint
- npm run test:unit -- tests/auth/route-access-control.spec.ts tests/auth/session-expiry-redirect.spec.ts tests/platform/shell-role-isolation.spec.ts
</verification>

<success_criteria>
- AUTH-04 role access control is enforced for namespace and identity across navigation modes.
- AUTH-03 redirect and expiry behavior is complete and aligned with role-safe logout semantics.
</success_criteria>

<output>
After completion, create .planning/phases/02-authentication-access-control/02-04-SUMMARY.md
</output>
