---
phase: 02-authentication-access-control
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/components/auth/AuthRolePicker.vue
  - app/components/auth/AuthFormCard.vue
  - app/pages/auth/index.vue
  - app/pages/auth/nutritionist/index.vue
  - app/pages/auth/admin/index.vue
  - tests/auth/auth-gateway-neutrality.spec.ts
  - tests/auth/role-credential-login.spec.ts
autonomous: true
requirements:
  - AUTH-02
must_haves:
  truths:
    - "Nutritionist and super admin can sign in with email/password and land in role-correct namespaces."
    - "Auth gateway stays neutral and does not leak role-private navigation."
    - "Credential login errors are mapped to Persian recovery guidance."
  artifacts:
    - path: app/pages/auth/index.vue
      provides: neutral role-entry gateway with explicit role options
    - path: app/pages/auth/nutritionist/index.vue
      provides: nutritionist email/password form and submit flow
    - path: app/pages/auth/admin/index.vue
      provides: super admin email/password form and submit flow
  key_links:
    - from: app/pages/auth/nutritionist/index.vue
      to: /auth/login
      via: credential submit handler
      pattern: "POST /auth/login"
    - from: app/pages/auth/admin/index.vue
      to: /auth/login
      via: credential submit handler
      pattern: "POST /auth/login"
---

<objective>
Deliver role-specific credential authentication for nutritionist and super admin while preserving neutral shared auth entry.

Purpose: provide secure, role-correct email/password entry points and keep shared auth surfaces free of private-role leakage.
Output: role picker, credential form card, dedicated role auth pages, and automated tests for neutrality and role landing.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-access-control/02-CONTEXT.md
@.planning/phases/02-authentication-access-control/02-UI-SPEC.md
@.planning/phases/02-authentication-access-control/02-01-SUMMARY.md
@docs/API.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build neutral auth gateway with explicit role entry points</name>
  <files>app/components/auth/AuthRolePicker.vue, app/pages/auth/index.vue, tests/auth/auth-gateway-neutrality.spec.ts</files>
  <behavior>
    - Test 1: Gateway exposes exactly three role entry options and no role-private navigation links.
    - Test 2: Authenticated users are redirected to their role root when opening /auth.
  </behavior>
  <action>Replace generic auth index with a neutral role selector (client OTP, nutritionist credentials, admin credentials) while preventing any private-route leakage (per D-08). Keep Persian-only concise copy and recovery tone (per D-10).</action>
  <verify>
    <automated>npm run test:unit -- tests/auth/auth-gateway-neutrality.spec.ts</automated>
  </verify>
  <done>Shared auth gateway is explicit, neutral, and role-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement nutritionist and admin credential login pages</name>
  <files>app/components/auth/AuthFormCard.vue, app/pages/auth/nutritionist/index.vue, app/pages/auth/admin/index.vue, tests/auth/role-credential-login.spec.ts</files>
  <behavior>
    - Test 1: Both role forms submit to /auth/login and route successful sessions to their role namespace root.
    - Test 2: Invalid credential errors display controlled Persian message and preserve form context for retry.
  </behavior>
  <action>Implement email/password authentication screens for nutritionist and super admin only via /auth/login (per D-02), prohibit alternate methods (per D-03), and enforce role-targeted landing routes (/nutritionist, /admin).</action>
  <verify>
    <automated>npm run test:unit -- tests/auth/role-credential-login.spec.ts</automated>
  </verify>
  <done>Nutritionist and super admin can log in through role-specific credential flows with safe error UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public auth routes -> role-scoped routes | Public pages must not leak or pre-authorize private route access. |
| credential form input -> login endpoint | Untrusted credentials must be validated and safely handled in UI state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | E | auth gateway routing | mitigate | Restrict gateway targets to allowed internal role routes and reject arbitrary redirect params. |
| T-02-09 | I | credential failure rendering | mitigate | Map INVALID_CREDENTIALS and related errors to fixed Persian copy without account existence hints. |
| T-02-10 | R | login transitions | mitigate | Log deterministic client-side auth state transitions in store actions for reproducible audit during test runs. |
</threat_model>

<verification>
- npm run lint
- npm run test:unit -- tests/auth/auth-gateway-neutrality.spec.ts tests/auth/role-credential-login.spec.ts
</verification>

<success_criteria>
- AUTH-02 is complete across nutritionist and admin credential flows.
- Shared auth entry remains neutral and does not reveal role-private navigation or data.
</success_criteria>

<output>
After completion, create .planning/phases/02-authentication-access-control/02-03-SUMMARY.md
</output>
