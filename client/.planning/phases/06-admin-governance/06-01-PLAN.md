---
phase: 06-admin-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/types/admin.ts
  - app/types/catalogue.ts
  - app/composables/useAdminStatsApi.ts
  - app/composables/useAdminNutritionistApi.ts
  - app/composables/useAdminCatalogueApi.ts
  - tests/admin/admin-api-contracts.spec.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - "Admin stats, nutritionist management, and elevated catalogue endpoints are represented by typed contracts and composables before page implementation."
    - "Admin API wrappers call documented /api/v1/admin/* routes and do not reuse nutritionist-scoped assumptions."
    - "Phase 6 foundation remains online-only with no offline queue/store integration under admin flows."
  artifacts:
    - path: app/types/admin.ts
      provides: admin stats and nutritionist management contracts
      exports: ["AdminStats", "AdminNutritionist", "AdminNutritionistListResponse", "AdminNutritionistStatusRequest"]
    - path: app/types/catalogue.ts
      provides: elevated admin catalogue query and category governance contracts
      exports: ["AdminFoodSearchQuery", "AdminMedicationSearchQuery", "AdminCreateFoodCategoryRequest"]
    - path: app/composables/useAdminStatsApi.ts
      provides: stats endpoint wrapper
      exports: ["useAdminStatsApi"]
    - path: app/composables/useAdminNutritionistApi.ts
      provides: nutritionist create/list/detail/update/status/clients wrappers
      exports: ["useAdminNutritionistApi"]
    - path: app/composables/useAdminCatalogueApi.ts
      provides: elevated foods/medications/categories wrappers
      exports: ["useAdminCatalogueApi"]
    - path: tests/admin/admin-api-contracts.spec.ts
      provides: endpoint and method regression coverage for Phase 6 composables
  key_links:
    - from: app/composables/useAdminStatsApi.ts
      to: app/types/admin.ts
      via: typed stats response
      pattern: "AdminStats"
    - from: app/composables/useAdminNutritionistApi.ts
      to: app/types/admin.ts
      via: typed list/detail/status payloads
      pattern: "AdminNutritionist"
    - from: app/composables/useAdminCatalogueApi.ts
      to: app/types/catalogue.ts
      via: elevated admin query typing
      pattern: "Admin(Food|Medication)SearchQuery"
---

<objective>
Create the contract and composable foundation for all Admin Governance UI slices.

Purpose: lock endpoint-faithful interfaces and transport wrappers so UI plans can execute without re-discovering API contracts.
Output: admin domain types, elevated catalogue/admin composables, and contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-admin-governance/06-CONTEXT.md
@.planning/phases/06-admin-governance/06-RESEARCH.md
@.planning/phases/06-admin-governance/06-VALIDATION.md
@docs/API.md
@docs/PRD.md
@.planning/phases/05-nutritionist-workspace-plan-authoring/05-01-PLAN.md

<interfaces>
From app/composables/useCatalogueApi.ts:
```typescript
export const useCatalogueApi = () => {
  async function searchFoods(query: FoodSearchQuery = {}) {
    return useAsyncData(key, () =>
      $fetch<PaginatedCatalogueResponse<FoodItem>>(`${baseUrl}/foods${suffix}`)
    )
  }
}
```

From app/composables/useNutritionistClientApi.ts:
```typescript
export const useNutritionistClientApi = () => {
  async function listClients(query: NutritionistClientListQuery = {}) {
    return useAsyncData(key, () => $fetch<NutritionistClientListResponse>(`${baseUrl}/clients${suffix}`))
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define admin governance contracts in type layer</name>
  <files>app/types/admin.ts, app/types/catalogue.ts, tests/admin/admin-api-contracts.spec.ts</files>
  <behavior>
    - Test 1: Admin stats type includes only API-backed fields from GET /admin/stats per D-04 and D-04a.
    - Test 2: Nutritionist management types cover create/list/detail/update/status and read-only clients per D-05.
    - Test 3: Elevated catalogue and category governance payload/query types align with D-07, D-08, and D-09.
  </behavior>
  <action>Create the initial admin contract spec first in `tests/admin/admin-api-contracts.spec.ts`, then implement strict interfaces and query types for admin stats, nutritionist lifecycle, and elevated catalogue governance. Keep field names contract-faithful to docs/API.md and explicitly omit unsupported audit-log contracts per D-10.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin/admin-api-contracts.spec.ts</automated>
  </verify>
  <done>Type contracts compile and express all Phase 6 backend capabilities without unsupported fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin stats and nutritionist composables</name>
  <files>app/composables/useAdminStatsApi.ts, app/composables/useAdminNutritionistApi.ts</files>
  <behavior>
    - Test 1: Stats composable calls GET /api/v1/admin/stats and returns typed data envelope per D-04.
    - Test 2: Nutritionist composable exposes create/list/get/update/setStatus/listClients with /api/v1/admin/nutritionists* routes per D-05.
  </behavior>
  <action>Implement composables with baseUrl '/api/v1' and useAsyncData/$fetch patterns consistent with prior phases. Enforce explicit status mutation payloads for PATCH /status to support confirmation workflows per D-06, and keep all operations online-only per D-01.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin/admin-api-contracts.spec.ts</automated>
  </verify>
  <done>Admin stats and nutritionist endpoint wrappers are complete, typed, and ready for page integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement elevated admin catalogue composable and contract tests</name>
  <files>app/composables/useAdminCatalogueApi.ts, tests/admin/admin-api-contracts.spec.ts</files>
  <behavior>
    - Test 1: Elevated food search/delete methods use /api/v1/admin/foods and /api/v1/admin/foods/:id per D-07.
    - Test 2: Elevated medication search/delete methods use /api/v1/admin/medications and /api/v1/admin/medications/:id per D-08.
    - Test 3: Category governance methods use /api/v1/admin/food-categories create/delete per D-09.
  </behavior>
  <action>Add admin-specific catalogue wrapper methods and contract assertions that fail if non-admin endpoints are used for governance paths (D-03). Keep audit-log methods absent by design per D-10.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin/admin-api-contracts.spec.ts</automated>
  </verify>
  <done>Elevated catalogue composable is endpoint-accurate and contract-tested for ADMIN-02.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI -> admin API endpoints | Super-admin-triggered create/update/delete/status payloads cross into privileged backend routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Elevation of Privilege | useAdminCatalogueApi | mitigate | Hardcode elevated governance calls to /admin routes and cover with endpoint-contract tests. |
| T-06-01-02 | Tampering | useAdminNutritionistApi status mutation | mitigate | Require explicit typed body { is_active } and reject implicit state toggles at UI boundary. |
| T-06-01-03 | Repudiation | privileged mutation wrappers | accept | Frontend cannot provide server audit log in current contract; expose clear confirmation UX and response messaging, defer audit-log UI per D-10. |
</threat_model>

<verification>
1. `npm run test:unit -- tests/admin/admin-api-contracts.spec.ts`
2. `npm run test:unit -- tests/auth/route-access-control.spec.ts`
3. `npx tsc --noEmit`
</verification>

<success_criteria>
- ADMIN-01 and ADMIN-02 contracts are represented in type/composable layers.
- Elevated governance endpoints are encoded and regression-tested.
- No unsupported audit-log or offline admin behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-governance/06-01-SUMMARY.md`
</output>
