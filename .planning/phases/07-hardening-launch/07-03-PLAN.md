---
phase: 07-hardening-launch
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - docker\observability\loki-config.yml
  - docker\observability\promtail-config.yml
  - docker\observability\grafana\datasources\loki.yml
  - docker\observability\grafana\provisioning\dashboards\dashboards.yml
  - docker\observability\grafana\dashboards\nutritrack-overview.json
autonomous: true
requirements:
  - INFRA-05
  - INFRA-11
must_haves:
  truths:
    - "docker-compose.yml provisions Grafana, Loki, and Promtail alongside the app stack"
    - "Grafana is pre-provisioned with a Loki datasource and a NutriTrack dashboard artifact"
    - "Observability env/config paths are documented in repo files rather than hidden manual setup"
---

## Context

Phase 7 must make the self-hosted monitoring story real in Docker Compose. The codebase already logs JSON to stdout, so the missing work is wiring the observability stack and shipping baseline dashboards.

## Objective

Add repository-owned observability infrastructure for Loki + Grafana and wire it into the existing Docker Compose deployment.

## Tasks

<task>
  <name>Wire Loki, Promtail, and Grafana into Compose</name>
  <read_first>
    - docker-compose.yml
    - docker-compose.dev.yml
    - backend\internal\middleware\logger.go
  </read_first>
  <action>
    Extend docker-compose.yml with loki, promtail, and grafana services, named volumes for Grafana/Loki state, and mounts for provisioning/config files. Keep the existing app services intact, retain restart: unless-stopped, and ensure Promtail can read Docker container logs or mounted log paths suitable for the current host topology.
  </action>
  <acceptance_criteria>
    - docker-compose.yml contains loki, promtail, and grafana service blocks
    - docker-compose.yml declares volumes for grafana and loki state
    - docker-compose.yml still contains api, frontend, postgres, and traefik
  </acceptance_criteria>
</task>

<task>
  <name>Create provisioning configs and baseline dashboard artifacts</name>
  <read_first>
    - backend\internal\middleware\logger.go
    - .planning\phases\07-hardening-launch\07-RESEARCH.md
  </read_first>
  <action>
    Add Loki and Promtail config files under docker\observability plus Grafana datasource/dashboard provisioning YAML. Create a NutriTrack overview dashboard JSON that visualizes request rate, status-code errors, and duration_ms-derived latency from the existing JSON logs.
  </action>
  <acceptance_criteria>
    - docker\observability\loki-config.yml exists
    - docker\observability\promtail-config.yml exists
    - docker\observability\grafana\datasources\loki.yml exists
    - docker\observability\grafana\dashboards\nutritrack-overview.json exists
  </acceptance_criteria>
</task>

<task>
  <name>Document required observability environment values</name>
  <read_first>
    - .env.example
    - docker-compose.yml
  </read_first>
  <action>
    Update .env.example with any Grafana admin, alert, or backup-related defaults required by the new services while preserving existing app variables. Keep names explicit and production-oriented.
  </action>
  <acceptance_criteria>
    - .env.example contains Grafana-related variables
    - .env.example still contains DOMAIN, FRONTEND_URL, DATABASE_URL, JWT_SECRET, and UPLOADS_DIR
  </acceptance_criteria>
</task>

## Verification

- `docker compose config` renders successfully
- Provisioning files exist and are mounted from docker-compose.yml

## Success Criteria

- Monitoring stack is repository-owned and bootable via Compose
- Grafana has a prewired Loki datasource and NutriTrack dashboard artifact
- Observability configuration is explicit and reproducible
