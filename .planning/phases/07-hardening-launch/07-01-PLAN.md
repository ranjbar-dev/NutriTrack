---
phase: 07-hardening-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend\internal\middleware\security_headers.go
  - backend\internal\middleware\cors.go
  - backend\internal\handler\auth_handler.go
  - backend\internal\middleware\security_headers_test.go
  - backend\internal\middleware\cors_test.go
  - backend\internal\handler\auth_handler_test.go
  - backend\internal\handler\authorization_audit_test.go
autonomous: true
requirements:
  - INFRA-09
  - INFRA-11
must_haves:
  truths:
    - "Responses include HSTS plus stricter security headers without breaking existing behavior"
    - "Credentialed CORS only allows the configured frontend origin and rejects other origins for browser preflight requests"
    - "Auth cookies set SameSite=Strict in addition to Secure+HttpOnly"
    - "A backend audit test suite covers cross-tenant denial expectations for representative nutritionist/client endpoints"
---

## Context

Phase 7 starts with high-risk launch blockers: browser-facing hardening and row-level authorization evidence. This plan should fix only concrete hardening gaps and add automated evidence, not rewrite stable business logic.

## Objective

Tighten application security defaults and add repeatable authorization evidence for the most sensitive route families before launch.

## Tasks

<task>
  <name>Harden headers, CORS, and auth cookies</name>
  <read_first>
    - backend\internal\middleware\security_headers.go
    - backend\internal\middleware\cors.go
    - backend\internal\handler\auth_handler.go
    - backend\cmd\api\main.go
  </read_first>
  <action>
    Add Strict-Transport-Security and any missing no-sniff / framing / referrer protections in security_headers.go. Update cors.go so Access-Control-Allow-Origin is only emitted when the request Origin matches cfg.FrontendURL, include Vary: Origin, and return 403 for mismatched preflight origins. Update auth_handler.go to call SetSameSite(http.SameSiteStrictMode) before writing access_token and refresh_token cookies, and preserve the existing Secure + HttpOnly + path scoping.
  </action>
  <acceptance_criteria>
    - backend\internal\middleware\security_headers.go contains Strict-Transport-Security
    - backend\internal\middleware\cors.go contains Vary and origin equality checks
    - backend\internal\handler\auth_handler.go contains SetSameSite
    - go test ./... exits 0
  </acceptance_criteria>
</task>

<task>
  <name>Add focused unit tests for the hardening changes</name>
  <read_first>
    - backend\internal\middleware\security_headers.go
    - backend\internal\middleware\cors.go
    - backend\internal\handler\auth_handler.go
    - backend\internal\service\push_service_test.go
  </read_first>
  <action>
    Create security_headers_test.go and cors_test.go to assert the new headers and origin handling. Add auth_handler_test.go to verify Set-Cookie behavior still writes the same cookie names/paths and now includes SameSite=Strict. Reuse existing testify + httptest conventions already used in backend tests.
  </action>
  <acceptance_criteria>
    - backend\internal\middleware\security_headers_test.go exists
    - backend\internal\middleware\cors_test.go exists
    - backend\internal\handler\auth_handler_test.go exists
    - go test ./... exits 0
  </acceptance_criteria>
</task>

<task>
  <name>Add authorization audit coverage for representative cross-tenant routes</name>
  <read_first>
    - backend\cmd\api\main.go
    - backend\internal\handler\tracking_handler.go
    - backend\internal\handler\communication_handler.go
    - backend\internal\handler\client_handler.go
    - backend\internal\handler\diet_plan_handler.go
  </read_first>
  <action>
    Create backend\internal\handler\authorization_audit_test.go with table-driven httptest coverage for representative route families that accept clientId, partnerId, or plan identifiers. Verify unauthorized or cross-tenant requests are rejected with 401/403 and that the dangerous shared read/download endpoints still require authenticated ownership-aware flows. Keep the tests lightweight by focusing on middleware/handler contract surfaces and explicit denial behavior.
  </action>
  <acceptance_criteria>
    - backend\internal\handler\authorization_audit_test.go exists
    - authorization_audit_test.go contains a table of audited endpoints
    - go test ./... exits 0
  </acceptance_criteria>
</task>

## Verification

- `cd backend && go test ./...`
- Inspect emitted CORS headers with an allowed and disallowed Origin
- Confirm Set-Cookie includes `SameSite=Strict`

## Success Criteria

- Browser-facing hardening gaps are closed
- Automated tests protect the new hardening behavior
- Authorization evidence exists for sensitive multi-tenant routes
