---
phase: 07-hardening-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend\internal\repository\diet_plan_repo_test.go
  - backend\internal\repository\tracking_repo_test.go
  - backend\tools\loadtest\api-smoke.js
  - frontend\nuxt.config.ts
  - frontend\package.json
  - frontend\app\layouts\client.vue
autonomous: true
requirements:
  - INFRA-09
  - INFRA-11
  - UI-08
must_haves:
  truths:
    - "The diet plan aggregate SLA has a real benchmark/test instead of a skipped stub"
    - "Phase 7 produces a repeatable performance check artifact for backend latency and frontend startup"
    - "Client PWA chrome is slightly tightened to reduce startup noise and improve update/install clarity"
---

## Context

Performance hardening must prove the existing product can meet launch targets. Prefer filling existing benchmark/test gaps and creating lightweight repeatable measurement artifacts over introducing new runtime dependencies.

## Objective

Add concrete performance validation artifacts for API latency, diet plan load time, and initial PWA startup.

## Tasks

<task>
  <name>Replace the skipped diet plan performance stub</name>
  <read_first>
    - backend\internal\repository\diet_plan_repo_test.go
    - backend\internal\repository\tracking_repo_test.go
    - docs\phases.md
  </read_first>
  <action>
    Replace TestPlanAggregateLoadTime's t.Skip with a real benchmark or integration-style assertion that measures the aggregate call with realistic fixture setup and a 500ms threshold. Reuse the existing integration-test pattern already present in repository tests so the command stays within the current Go test ecosystem.
  </action>
  <acceptance_criteria>
    - backend\internal\repository\diet_plan_repo_test.go no longer contains t.Skip("stub
    - diet_plan_repo_test.go contains a duration assertion or benchmark
    - cd backend && go test ./... exits 0
  </acceptance_criteria>
</task>

<task>
  <name>Create a lightweight load/perf script artifact</name>
  <read_first>
    - backend\cmd\api\main.go
    - frontend\package.json
    - docs\phases.md
  </read_first>
  <action>
    Add backend\tools\loadtest\api-smoke.js with a small, documented request mix for health/auth/read endpoints that can be executed in launch verification. Keep it dependency-light and repository-local. Update frontend or docs-adjacent package metadata only if needed to expose a repeatable performance command without pulling a new production dependency into runtime code.
  </action>
  <acceptance_criteria>
    - backend\tools\loadtest\api-smoke.js exists
    - api-smoke.js contains staged request groups or scenarios
    - file text mentions p95 or latency thresholds
  </acceptance_criteria>
</task>

<task>
  <name>Tighten PWA startup chrome for launch measurement</name>
  <read_first>
    - frontend\app\layouts\client.vue
    - frontend\nuxt.config.ts
    - .planning\phases\07-hardening-launch\07-UI-SPEC.md
  </read_first>
  <action>
    Make the install/update banners and startup layout align with the UI-SPEC launch wording and visual-stability guidance. Keep the manifest and existing PWA setup, but remove any avoidable layout-shift sources in the client layout and ensure the updated banner copy matches the approved Persian strings.
  </action>
  <acceptance_criteria>
    - frontend\app\layouts\client.vue contains the polished update banner copy
    - frontend\nuxt.config.ts still builds successfully
    - cd frontend && npm run build exits 0
  </acceptance_criteria>
</task>

## Verification

- `cd backend && go test ./...`
- `cd frontend && npm run build`
- Inspect the new perf artifact and benchmark/test for documented thresholds

## Success Criteria

- Phase 7 has repeatable performance evidence artifacts
- The diet plan load target is no longer represented by a skipped placeholder
- PWA startup/polish work aligns with UI-08 launch expectations
